/*

Javascript - Linguagem é de Programação

// Armazenar Dados e Executar Instruções

// Camada de Armazenamento

var chave = "valor";
let chave = "valor";
const chave = "valor";

// Camada de Instruções

instrucao();
instrucao(param);
instrucao(param1, param2);

contexto.instrucao();
contexto.instrucao(param);
contexto.instrucao(param1, param2);

*/

let escola = "Caelum";
// var escola = "Alura"; // não é possível redeclarar
escola = "Alura"; // é possível reatribuir o valor

const fundacao = 2000;
// fundacao = 1985; // não é possível reatribuir o valor

// tipos primitivos
const nome = "Leonardo"; // string
const idade = 38; // number
const magro = false; // boolean
let nulo = null; // null
let indefinido; // undefined

console.log("=== VARIAVEIS ===");
console.log(nome, idade, magro);
console.log(nome, typeof nome);
console.log(idade, typeof idade);
console.log(magro, typeof magro);
console.log("");
